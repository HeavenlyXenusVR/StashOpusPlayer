<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <!-- Seal packages (preferred external downloader) -->
        <package android:name="com.junkfood.seal" />
        <package android:name="com.junkfood.seal.beta" />
        <package android:name="com.junkfood.seal.debug" />

        <!-- Allow resolving handlers for YouTube links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="www.youtube.com" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="m.youtube.com" />
        </intent>

        <!-- Allow resolving share intents for text URLs -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" 
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".StashOpusApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.StashOpusPlayer"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.StashOpusPlayer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="audio/*" />
            </intent-filter>

            <!-- Discord OAuth2 verification callback: the bridge server always
                 redirects here after the code exchange (main.py's
                 _DISCORD_CALLBACK_DEEP_LINK is a fixed constant, not
                 per-client-configurable), matching the exact scheme/host
                 Lumisound's iOS build also registers. See
                 DiscordVerificationScreen/DiscordVerificationViewModel. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lumisound" android:host="discord-verify" />
            </intent-filter>

            <!-- Discord OAuth2 SIGN-IN callback: distinct deep link from the
                 linking flow above. The bridge distinguishes "sign in with
                 Discord" from "link Discord to my account" by decoding the
                 OAuth state token, and redirects to a different deep link
                 per purpose (main.py's _DISCORD_LOGIN_CALLBACK_DEEP_LINK vs.
                 _DISCORD_CALLBACK_DEEP_LINK). See BridgeSettingsViewModel's
                 startDiscordSignIn()/DiscordLoginEvents. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lumisound" android:host="discord-login" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.NowPlayingActivity"
            android:exported="false"
            android:theme="@style/Theme.StashOpusPlayer" />

        <activity
            android:name=".ui.QueueActivity"
            android:exported="false"
            android:theme="@style/Theme.StashOpusPlayer" />

        <activity
            android:name=".ui.MetadataActivity"
            android:exported="false"
            android:theme="@style/Theme.StashOpusPlayer" />
        
        <activity
            android:name=".ui.YouTubeStreamingActivity"
            android:exported="false"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/Theme.StashOpusPlayer" />
        
        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <!-- Home screen widget provider -->
        <receiver
            android:name=".widgets.PlayerWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.stash.opusplayer.widgets.PLAY_PAUSE" />
                <action android:name="com.stash.opusplayer.widgets.NEXT" />
                <action android:name="com.stash.opusplayer.widgets.PREV" />
                <action android:name="com.stash.opusplayer.widgets.REWIND_10" />
                <action android:name="com.stash.opusplayer.widgets.FF_10" />
                <action android:name="com.stash.opusplayer.widgets.UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info" />
        </receiver>
        
        <!-- FileProvider for update installations -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
        
    </application>

</manifest>
